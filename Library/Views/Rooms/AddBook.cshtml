@using Library.Models;
<div class="modal fade" id="addBookToRoom" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Add Book</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="row books-row">
          <input type="hidden" name="room" value=@ViewData["roomId"]>
          <input type="hidden" name="shelf" value=@ViewData["shelfId"]>
          <div id="books-to-add" class="row books-row"></div>
          <nav id="r-pagination" class="mt-3">
            <ul class="pagination justify-content-center">
              <li class="page-item disabled">
                <a class="page-link" id="books-page-prev"><i class="fa fa-caret-left"></i></a>
              </li>
              <li class="page-item">
                <a class="page-link" id="books-page-next"><i class="fa fa-caret-right"></i></a>
              </li>
            </ul>
          </nav>
          @* @foreach (Book book in @ViewBag.Books)
          {
            <div class="card">
              @using (Html.BeginForm("Delete", "Books", new { id = ${book.bookId}},FormMethod.Post ))
              {
                <button type="submit" class="btn btn-light delete-book">x</button>
              }
              <img class="card-img-top" src="https://books.google.com/books/content?id=@(book.ImgID)&printsec=frontcover&img=1&zoom=5" alt="Book thumbnail" height="240px" object-fit="contain">
              <div class="card-body">
                <h5 class="card-title cut-text">${book.title}</h5>
                <p class="card-text cut-text">${book.authors}</p>
                @using (Html.BeginForm("AddLocation", "Books", new { id = ${book.bookId}, shelfId = ViewData["shelfId"], roomId = ViewData["roomId"]},FormMethod.Post ))
                {
                  <button class="btn btn-light" type="submit">Add</button>
                }
              </div>
            </div>
          } *@
        </div>
      </div>
    </div>
  </div>
</div>